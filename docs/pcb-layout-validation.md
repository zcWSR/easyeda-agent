# PCB 组合布局开发验证清单

当前目标：**保留已经做好的组内相对布局，以完整电路块为单位调整整板位置，再计算块间
布线通道，必要时反馈让位。** 同网不代表同组；整体移动不是再次打散摆放外围器件。

本页是待办与验收入口，算法调研见 [Layout/Router 研究](pcb-layout-solver-research.md)，
术语见 [概念表](concepts.md#样例参数与事实检查)。公开参数和可照做步骤维护在
[组合移动样例](../.agents/skills/easyeda-agent/references/examples/pcb-group-move/README.md)。

## 场景与测试如何配合

| 资料 | 回答的问题 | 保存什么 |
|---|---|---|
| 开发验证场景 | 用户要的能力在完整操作链上是否成立？ | 来源、起点、完整组、参数、独立要求、步骤、前后结果、未完成项 |
| 自动测试用例 | 改代码后，某条规则是否退化？ | 小输入、断言、正负例、确定的失败诊断 |
| 某次验证记录 | 哪个版本、哪些输入实际跑过？ | commit/工作树差异、输入与候选哈希、命令、报告、图、回读与清理结果 |

先写场景及不变量；为其中缺失能力补失败测试，修实现，运行回归；再按场景离线复现和现场
执行。已有能力先补可迁移的场景和证据，不故意改坏实现制造“红灯”。每轮只推进一个明确
范围；发现问题保留失败输入，修正后重跑该场景，不把 checklist 勾选当执行许可。

## 验收待办

下列复选框表示**整个场景**完成。自动测试和离线计算已有覆盖的项仍可能没有现场证据。
“已有测试”是源码覆盖映射，运行结果另记；没有整板实证的项目均保持待验收。

| 验收 | ID | 要验证的能力与独立判据 | 实现/自动覆盖现状 | 下一步 |
|---|---|---|---|---|
| [ ] | LG-01 | 已布局的核心、去耦、串阻、负载作为完整组平移；相对向量、角度、装配面、pad 网络保持；其他器件不动 | 五成员合成场景 `offline-verified`；CLI/公共包/Apply 对账通过 | 用真实无铜测试组做两轮持久化核对 |
| [ ] | LG-02 | 非对称 anchor 整组旋转；成员向量和所有 bbox/pad 同步绕指定 anchor 转，不能绕 bbox 中心 | 五成员 0°/90° 离线通过；平移后才能旋转的反馈漏解已修复 | 现场核对量化容差及位号；完整 pad/丝印预览仍待覆盖 |
| [ ] | LG-03 | 两个以上已布局块组合移动；每块内部关系保持，块之间不碰撞，未声明组保持 | 文件式 `multi-group-clearance` 离线通过，含双组让位和三条共同需求 | 建真实 MCU+外围、接口+保护组合场景，完整声明组间连接 |
| [ ] | LG-04 | 固定接口、开口、安装孔、固定轴、禁放区和凹板框约束保持 | 固定轴、异面占用、凹框等已有测试 | 在 LG-03 加固定板边与机械负例；不能只查外接矩形 |
| [ ] | LG-05 | 带内部导线/过孔/区域的完整组移动；局部回流真实连通，宽度、孔径、层不变 | 公共几何变换已有；新 solve 的内部铜 Apply 为 `unsupported` | 先补统一铜 Apply/回读；LDO 旧铜不得只留原位或删掉冒充通过 |
| [ ] | LG-06 | 位号、封装/3D 绑定、禁铺区域随正确 owner 保持；禁止复制或重绑掩盖遗漏 | 双成员位号随动曾回读；公共模型未完整表达丝印与绑定 | 采集全部身份与位号前后对账；缺字段明确 incomplete |
| [ ] | LG-07 | 移动后重算所有外部端点和连接；遗漏 demand、拉断既有外部铜必须识别 | 外部端点、遗漏连接、断开既有接触已有负例；外部旧铜替换未完成 | 用有真实外部端口的多组场景复现并补替换契约 |
| [ ] | LG-08 | 几何合法但两网竞争通道；分别可达不能误报共同可达 | `joint-channel-negative` 与 `automatic-channel` 离线案例已有 | 纳入 LG-03 完整连接，不只挑两条容易走的线 |
| [ ] | LG-09 | 试布失败反馈具体对象/网络/位置；完整组让位后共同通过；预留不写铜 | 二层闭环已有；ceshi 双网双成员 layout-only 曾持久化 | 多个真实电路块的整板组合移动仍待验证 |
| [ ] | LG-10 | 阻挡组递归让位，遇固定组回溯；状态去重，不循环试摆 | 递归让位单测及文件场景通过；失败/最终 SVG 与具体移动原因已输出 | 迁移到真实模块并保存两轮现场证据 |
| [ ] | LG-11 | 二层 TOP/BOTTOM 通道与过孔预算；TOP-only 不换层，通孔焊盘阻挡两层 | `two-layer-via` 等离线测试已有 | 记录孔铜面积/钻孔/净距及网络预算；现场写铜属于布局确认后的步骤 |
| [ ] | LG-12 | 缺测、未知铜、漏成员、重复所有权、篡改路径、过期快照不误报成功 | 多类负例已有；Apply 尚不自动核对来源语义哈希 | 执行前 fresh dump 对照；补 Apply 来源校验与部分失败恢复场景 |
| [ ] | LG-13 | 两轮整板自检；第 2 轮 save→reload→fresh dump→fresh render；与候选同版，未改对象保持 | 双网通道曾完成；整板组合场景未完成 | 对 LG-01～07 分别保存证据，任一修正则轮次清零 |
| [ ] | LG-14 | 预算耗尽保留已找到合法解，报告搜索未完成；耗尽不伪造阻挡归因 | 状态/网格预算测试已有 | 加耗时、内存、几十件/大型参考板；评分基准不算布通证据 |
| [ ] | LG-15 | 整体平移/旋转、输入乱序保持电气结论；参数迁移不依赖某块板坐标 | 公共反馈变形测试已有 | 扩展到多个已有组和实际 CLI 输入；期望是约束而非唯一坐标 |
| [ ] | LG-16 | 初始布局就估算全网方向/通道容量，随后精确共同试布 | `planned`，现有闭环只做布局后的修复 | LG-03/08/09 稳定后，对比“先布局再让位”基线 |
| [ ] | LG-17 | 四层/更多层的有序层、平面角色/网络、过孔跨度明确；不支持的联合求解明确拒绝 | 模型和二层拒绝边界已有；多层联合求解 `planned` | 按 [A4](pcb-solver-community-design-detail.md#a4多层布局布线协调) 同步设计，分模型/检查、多层搜索、平面/回流案例验收 |
| [ ] | LG-18 | 存量 Layout 全部抽包、旧命令/JSON 兼容；外部 Go 调用；ESP32 原始需求完整回归 | 四公共包已有，存量算法迁移与完整回归未完成 | 单独执行迁移与原始需求验收，不由本清单的小场景替代 |
| [ ] | LG-19 | 固定布局可共同布通时，回退先走的路径并改变排网顺序；不能因为需求 ID 的平局顺序就漏解 | 四固定器件的真实几何反例已修复并独立复验；原输入/重排/重命名均通过 | 扩展多通道候选和规模；仍不宣称穷举全部同层路线 |

顺序：LG-01/02 → LG-03/04 → LG-05/06/07 → LG-08/09/10 → LG-11/12/13/14/15。
LG-16 与 LG-17 纳入社区设计并分阶段实现；多层能力尚未通过验收，LG-18 随迁移实施。先选择无铜的组验证几何，再推进带铜组；
这两个结果单独记录，不能因无铜例通过就宣称电源模块整体移动已完成。
用户要求分析路线避让后，增加 LG-19 并在 LG-09 之前验证：共同 Router 应先尝试固定布局的
有界路线/排网回溯，再将实际剩余冲突反馈给 Layout。本轮也推进 LG-02 的平移后旋转与
LG-03/07/10 的合成场景，现场顺序仍按新鲜实测证据推进。

## 首项场景卡：LG-01 / LG-02

- **来源**：2026-09-23 用户提出“保留已有相对布局，验证整体器件组合移动”。五成员公开
  合成输入用于独立核对几何，非器件选型/BOM、非真实板测量、非完成态布局标准答案。
- **开始状态**：二层板，U1/C1/C2/R1/D1 已具有不同相对位置和角度；J1/H1 固定；无既有铜。
  U1 footprint anchor 不在 bbox 中心。成员和 pin owner 显式列出。
- **参数**：mil、y 向上、footprint anchor；整体 dx/dy、允许转角、最小净距、成员列表。
  给定偏移是用户要求的移动动作，本例不验证求解器是否会自己找到让位方向。
- **独立验收**：完整成员集合；所有成员相对 anchor 的向量经同一旋转；所有成员间距、
  相对角度、pad 身份/网络保持；非对称 bbox 和 pads 同步；固定对象不在 Apply 中；预览与
  Apply 使用同一候选。0°/90°分别核对，公用模型与宿主适配分别验证。
- **负例**：漏掉 C2 的输入与独立成员表不符；候选只挪 U1；固定 J1 出现在写入集；旋转绕
  bbox 中心；增加未声明铜。不能从已经缩小的 `members` 自证“完整”，成员表必须来自独立
  功能归属。通用“全部业务成员已声明”的自动检查能力也不能凭几何模型声称存在。
- **离线命令与参数**：见[公开场景步骤](../.agents/skills/easyeda-agent/references/examples/pcb-group-move/README.md)。
- **现场步骤（待执行）**：在 ceshi 专用无铜测试组 fresh dump；按实测重绑成员/所有权/PID；
  生成候选；fresh 来源对账；typed Apply；两轮自检与持久化回读。保存原始 DRC 作前后差异，
  不把整板既有错误清零当本场景的几何前提，也不忽略新错误。所有对象按完整数据对账，不能
  只比较计数。最后按 fresh 身份从原参数恢复测试对象，保存重载再核对。
- **证据包**：源码版本与差异、原板、独立成员/固定件/约束、参数、候选、前后图、自动测试
  输出、Apply journal、两轮 fresh 原始回读、逐对象差异、DRC 差异、清理结果。私有现场输入
  留在本地，公开回填可迁移参数与结论。
- **完成条件**：离线几何及 CLI 对账完成，只升级离线复现；上述现场证据完整后才勾选本项。
  内部铜、外部布通和整板布局质量分别由后续场景验收。

## 本轮执行记录（2026-09-24）

LG-01/02 的合成场景完成离线复现；详见
[运行记录](reviews/2026-09-24-pcb-group-move.md)。当前验证维度分别为：

| 维度 | 本轮结果 |
|---|---|
| 实现 | 已有 rigid/公共组变换；本轮未修改生产算法 |
| 自动回归 | 五成员 CLI/公共包/Apply 不变量及真实漏成员候选负例通过 |
| 离线复现 | 两个候选，0 拒绝；原图/平移/旋转已观察，旧图仅 bbox |
| 现场持久化 | 未执行；不勾选完整场景，不宣称多块整板布局完成 |

既有双网实验只计入 LG-09 的局部现场证据，不能借此勾选 LG-01～07 或整板 Layout。
当前 ceshi 仍有上一轮四个测试器件，下次现场操作先 fresh 读取并核对其身份；不得假定
已清理或直接复用旧 PID。下一步是 LG-01/02 的真实无铜组现场验证，再推进 LG-03 多块组合。

## 路线避让开发增量（2026-09-24）

组合移动提供保持组内关系的动作，实际通道占用与冲突反馈决定移动理由。两者配合可实现
路线避让；单看布局无重叠或已有五成员移动例，不能证明路径共同可用。分析发现并修复了
两个会漏解的搜索缺口，并把证据接入以下场景：

| 场景 | 不依赖输出坐标的验收 | 本轮离线结果 |
|---|---|---|
| LG-19 固定布局路线回溯 | 两网分别可达；独立共同路径 witness 合法；求解保持全部固定器件，0 移动/0 新过孔；打乱输入与重命名仍共同通过 | `fixed-route-order` 已通过；修改前原输入失败，仅改 demand ID 就成功 |
| LG-02/09 平移后整组旋转 | 原地旋转碰固定件，单纯平移仍阻挡；允许转角后找到共同路径；组内成员/内部几何同一原始 anchor 投影；禁止旋转时保持失败 | `TestRouteAvoidanceNeedsTranslationThenWholeGroupRotation` 与公共邻域测试已通过 |
| LG-03/07/10 双组递归让位 | 初始机械合法但路由受阻；两个完整组保持相对布局；外部端点随移动重算；三网共同检查，固定物/既有铜/规则保持 | `multi-group-clearance` 已通过；固定第二组、漏外部 demand 负例通过 |

代码输入与期望分别见
[固定布局路线场景](../pkg/pcbsolve/testdata/fixed-route-order/README.md)、
[双组场景](../pkg/pcbsolve/testdata/multi-group-clearance/README.md)。两者都是明确范围的合成
几何，不是 MCU/LDO 真实模块验收。联合路径先在固定布局中回溯，Layout 的反馈邻域再探索
声明平移和旋转，全部尝试共用预算；搜索穷尽当前候选不等于所有物理方案不可行。

整板/局部 SVG 与报告同源，显示真实模型 pads、路径宽度/净距、移动前位置、移动原因和
请求机械禁放区；独立 `render --board --candidate` 缺请求时不能恢复仅在需求中的禁区。
加 `--from request.json` 即可来源对账并显示同一约束。
完整记录与验证结果见 [路线避让运行记录](reviews/2026-09-24-pcb-route-avoidance.md)。
