# v1.6.0 测试基准（进行中）

- **唯一客户输入**：[`esp32MiniRequire.md` 的「一、客户原始需求」](../../../../esp32MiniRequire.md#一客户原始需求)。按 UTF-8 从标题 `## 一、客户原始需求` 起，到第二节前连续两条分隔线前止，SHA256 为 `e62ebb05ecedb82e103d81b31da2bbf26aefe0669e580961c3aae87fef7d74d0`。执行 Agent 不接收本文件的验收表、预制 BOM、UUID、网表或目标坐标。
- **现场起点**：用户登录的 Web EasyEDA Pro 4.1.60，专用项目 `easyeda-v160-schematic-test-20260923`（`cc931a5b30a0447e8886d6b62e975ccf`），首次运行由空白原理图开始。当前恢复测试前，P1 已部分写入、P2/P3 空白；不能把恢复态冒充空白起点。
- **待测运行时**：当前本地 CLI、daemon、Skill、连接器 `1.6.0-dev.5`，候选提交 `ab67abe`；以每轮 `easyeda health` 的实际运行版本及目标工程/文档为准。用户刷新后 P1/P2/P3 页切换保持同一运行窗口。正式发布候选仍须重新固定版本及哈希，不能沿用开发版结果。
- **版本化源与事实**：所有布局以原始官方快照、源数据、27+4 件实测位号几何、参数和候选哈希重算；清页前完整图元状态守卫。宿主属性 `KeyVisible` 返回 `undefined` 时必须拒绝，不得补默认值。每页采用新鲜快照，写入后显式保存、真实重载、新鲜对象回读和 DRC。截图只辅助发现问题。
- **通过阈值**：客户要求逐条映射，所有物理脚有可信 pin→net/NC；核心及专属外围整体移动，范围外对象不变；位号/导线/框/图签无碰撞；逐页 strict gate 通过、0 overlap、0 fatal、网络连通，保存重载后保持；完整固定端到端用例按 S0–S6 + P0–P10 验收 4 层电源树、丝印极性、天线 keepout、已落盘。原理图专项完成只算专项通过，不能代替整板通过。
- **当前结论**：基准已固定，现场测试未完成；不得生成 `pass` 发布清单。

详细失败与证据见[当日现场记录](../../../reviews/2026-09-23-v1.6.0-schematic-acceptance.md)。
